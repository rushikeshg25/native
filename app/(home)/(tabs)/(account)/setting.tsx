import { View } from 'react-native';
import { Text } from 'react-native-paper';

export default function Setting() {
  return (
    <View className='flex items-center justify-center mb-10'>
      <Text>Account setting page</Text>
    </View>
  );
}
